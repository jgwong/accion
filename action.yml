name: 'Accion Super'
description: 'My new Action'
runs:
  using: "composite"
  steps:
      - name: Install dependencies
        run: |
          echo $SSH_KEY
        shell: bash
        env:
          SSH_KEY: ${{ secrets.KEY }}

